import React from 'react'

/* Organinsms */
import { Header, PhotosGrid } from '@components/organisms'

const Home = (): JSX.Element => {
   return (
      <React.Fragment>
         <Header />
         <PhotosGrid />
      </React.Fragment>
   )
}

export default Home
