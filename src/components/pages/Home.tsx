import React from 'react'

/* Organinsms */
import { Header, PhotoGrid } from '@components/organisms'

const Home = (): JSX.Element => {
   return (
      <React.Fragment>
         <Header />
         <PhotoGrid />
      </React.Fragment>
   )
}

export default Home
